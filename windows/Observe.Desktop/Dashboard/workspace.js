'use strict';
let networkSnapshot=null,networkTab='flows',networkSearch='',networkMinutes=60,networkLoading=false,networkApp='',networkPageIndex=0;
let trackedList=[],trackedView=null,trackingLoading=false;
const threatChecks=new Map();
names.launches='Tracked apps';
function overviewPage(){return heading('Your PC, in view.','Start a focused recording, review saved evidence, or pause monitoring.')+`<div class="main-grid"><div class="column"><section class="observer-welcome"><div class="eyebrow">FOLLOW AN APP FROM THE START</div><h2>What does this game do?</h2><p>Launch a game or installer through Observe. Follow its child processes, IP connections and recorded changes in one saved session. For mods, launch the game after installing the mod and compare recordings.</p><button class="primary" data-page="launches">Launch & observe <span>↗</span></button><small>Runs on your PC · Observation is not isolation or a malware verdict</small></section><section class="card"><div class="card-head"><h2>Ask about your evidence</h2></div><div class="card-body"><p class="card-note">Open a tracked recording to inspect suspicious behavior, or ask chatGPT observer to explain the evidence.</p><button class="secondary" data-page="observer">Open chatGPT observer ↗</button></div></section><section class="card"><div class="card-head"><h2>Game performance</h2></div><div class="card-body"><p class="card-note">Restart with Observe's collectors off. Observe-installed Sysmon is removed temporarily; pre-existing Sysmon and PowerShell settings are restored from the saved backups. Restore monitoring brings Observe's sensor configuration back.</p><p class="card-note">Recordings are saved before restarting. Monitoring gaps cannot be recovered. Existing external monitoring can remain active.</p><button class="secondary" data-action="performance">${state.data.gaming?'Restore monitoring & restart':'Game performance & restart'}</button></div></section></div><div class="column"><section class="card"><div class="card-head"><h2>Sensor coverage</h2><span class="tag">LOCAL</span></div><div class="card-body" id="coverage"></div></section><section class="card"><div class="card-head"><h2>A quieter workspace</h2></div><div class="card-body"><p class="card-note">Network opens a frozen snapshot. Use Refresh when you want the latest Flows or app Activity. Observation continues in the background while monitoring is on.</p><button class="secondary" data-page="network">Review network →</button></div></section></div></div>`;}
function overviewUpdate(d){const sysmon=d.sensors.channels.find(c=>c.includes('Sysmon'))||d.sensors.channels[0]||'Unavailable';$('#coverage').innerHTML=[['Connections',d.sensors.tcp],['Domain evidence',d.sensors.dns],['Sysmon events',sysmon],['PowerShell logging',d.sensors.policy]].map(([title,text])=>`<div class="coverage-item"><b class="coverage-dot ${d.gaming||/unavailable|unauthorized|denied|not enabled/i.test(text)||title==='Sysmon events'&&!text.includes('verified')?'warn':''}"></b><div><strong>${title}</strong><small>${esc(text)}</small></div></div>`).join('')+(d.gaming?'<span class="tag warn">Collectors off</span>':loggingControl()+(!d.administrator?'<button class="secondary" data-action="elevate">Read logs as administrator ↗</button>':''));}
function sizeText(n){if(n==null)return '—';if(n<1024)return n+' B';if(n<1048576)return (n/1024).toFixed(1)+' KB';if(n<1073741824)return (n/1048576).toFixed(1)+' MB';return (n/1073741824).toFixed(2)+' GB';}
function appIcon(icon,name){return icon?`<img class="app-icon" alt="" src="${esc(icon)}">`:`<span class="app-icon app-initial" aria-hidden="true">${esc((name||'?')[0].toUpperCase())}</span>`;}
function countryFlag(code,scope){const designs={DK:'<path fill="#c8102e" d="M0 0h30v20H0z"/><path fill="#fff" d="M9 0h3v20H9zM0 8h30v3H0z"/>',DE:'<path d="M0 0h30v7H0z"/><path fill="#d00" d="M0 7h30v7H0z"/><path fill="#fc0" d="M0 14h30v6H0z"/>',FR:'<path fill="#002395" d="M0 0h10v20H0z"/><path fill="#fff" d="M10 0h10v20H10z"/><path fill="#ed2939" d="M20 0h10v20H20z"/>',NL:'<path fill="#ae1c28" d="M0 0h30v7H0z"/><path fill="#fff" d="M0 7h30v7H0z"/><path fill="#21468b" d="M0 14h30v6H0z"/>',US:'<path fill="#fff" d="M0 0h30v20H0z"/><path stroke="#b22234" stroke-width="1.54" d="M0 .77h30M0 3.85h30M0 6.92h30M0 10h30M0 13.08h30M0 16.15h30M0 19.23h30"/><path fill="#3c3b6e" d="M0 0h13v10.77H0z"/><path stroke="white" stroke-dasharray="1 1.5" d="M1 2h11M1 4h11M1 6h11M1 8h11"/>'};return code?`<span class="country" title="${esc(code)} · imported country map">${designs[code]?'<svg viewBox="0 0 30 20" aria-hidden="true">'+designs[code]+'</svg>':esc(code)}</span>`:`<span class="country" title="${esc(scope)} · country unknown">${scope==='Local / reserved'?'⌂':'◎'}</span>`;}
function networkPage(){return heading('Network, at your pace.','Flows and the apps behind them. The view stays still until you refresh.')+`<div class="network-workspace"><aside class="network-sidebar"><div class="segmented"><button data-net-tab="flows" class="${networkTab==='flows'?'selected':''}">⇄ Flows</button><button data-net-tab="activity" class="${networkTab==='activity'?'selected':''}">▥ Activity</button></div><label class="field">Evidence period<select id="net-period"><option value="5">Last 5 minutes</option><option value="60">Last hour</option><option value="1440">Last 24 hours</option></select></label><label class="field">Search<input id="net-search" placeholder="App, IP or domain" value="${esc(networkSearch)}"></label><button class="primary" data-action="network-refresh" id="network-refresh">↻ Refresh snapshot</button><p id="network-stamp" class="small muted"></p><div class="network-side-note"><strong>Snapshot view</strong><p>Background capture continues while monitoring is on. No scrolling or row updates are automatic.</p></div><button class="quiet" data-action="country-import">Import country map</button><small class="muted">Optional local CSV: CIDR,country. No geolocation uploads.</small></aside><section class="network-main"><div id="network-summary"></div><div id="network-chart"></div><section class="card"><div class="card-head"><h2 id="network-title">Flows</h2><button class="quiet" id="network-clear-app" data-action="network-clear-app"></button></div><div class="table-wrap"><table class="data-table flow-table"><thead id="network-columns"></thead><tbody id="network-rows"></tbody></table></div><div class="table-foot network-pagination"><button class="quiet" data-action="network-prev">← Previous</button><span id="network-count"></span><button class="quiet" data-action="network-next">Next →</button></div></section><details class="network-coverage"><summary>Coverage & attribution</summary><div id="network-warnings"></div></details></section></div>`;}
async function loadNetwork(){if(networkLoading)return;networkLoading=true;try{networkSnapshot=await request('network-snapshot',{minutes:networkMinutes});networkPageIndex=0;if(page==='network')renderNetwork();}catch(e){toast(e.message,true);}finally{networkLoading=false;}}
function netRisk(indicator){const r=threatChecks.get(indicator);return r?(r.status==='match'?'<span class="risk-match">IOC match</span>':'<span class="tag">No match</span>'):'<span class="muted">Not checked</span>';}
function renderNetwork(){if(page!=='network'||!$('#network-rows'))return;$('#net-period').value=String(networkMinutes);document.querySelectorAll('[data-net-tab]').forEach(b=>b.classList.toggle('selected',b.dataset.netTab===networkTab));if(!networkSnapshot){$('#network-rows').innerHTML='<tr><td class="empty">Loading a snapshot…</td></tr>';return;}const s=networkSnapshot;$('#network-stamp').textContent='Captured '+date(s.capturedAt)+' · '+time(s.capturedAt);$('#network-clear-app').textContent=networkApp?'Clear app filter ×':'';const totalDown=s.apps.some(a=>a.download!=null)?s.apps.reduce((n,a)=>n+(a.download||0),0):null,totalUp=s.apps.some(a=>a.upload!=null)?s.apps.reduce((n,a)=>n+(a.upload||0),0):null;const dest=new Map();for(const f of s.flows){const key=f.remoteIp||f.domain||'Unknown';dest.set(key,(dest.get(key)||0)+1);}const top=[...dest].sort((a,b)=>b[1]-a[1]).slice(0,3);$('#network-summary').innerHTML=`<div class="network-summary-grid"><article><span>FLOW SUMMARY</span><h2>${s.flows.length.toLocaleString()}</h2><p>${s.apps.filter(a=>a.pids.length>0).length} apps · ${dest.size} destinations</p></article><article><span>OBSERVED TRANSFER</span><h2>↓ ${sizeText(totalDown)}</h2><p>↑ ${sizeText(totalUp)} · ETW only</p></article><article><span>TOP DESTINATIONS</span>${top.map(([name,n])=>`<div class="top-destination"><span>${esc(name)}</span><b>${n}</b></div>`).join('')||'<p>No destinations recorded</p>'}</article></div>`;
$('#network-title').textContent=networkTab==='activity'?'App Activity':'Flows';$('#network-chart').innerHTML=networkTab==='activity'?trafficChart(s):'';let rows;
if(networkTab==='flows'){$('#network-columns').innerHTML='<tr><th>APP / PROCESS</th><th>DESTINATION</th><th>SERVICE</th><th>OBSERVED BYTES</th><th>INTELLIGENCE</th><th>LAST SEEN</th></tr>';rows=s.flows.filter(f=>(!networkApp||f.image===networkApp)&&JSON.stringify(f).toLowerCase().includes(networkSearch));$('#network-rows').innerHTML=rows.slice(networkPageIndex*40,networkPageIndex*40+40).map(f=>{const app=s.apps.find(a=>a.image===f.image);const indicator=f.remoteIp||f.domain.split(' (')[0];const lookup=indicator&&!indicator.includes(',')&&f.scope!=='Local / reserved';return `<tr><td><button class="app-cell" data-net-app="${esc(f.image)}">${appIcon(app?.icon,f.process)}<span>${esc(f.process)}<small>PID ${esc(f.pid&&f.pid!=='0'?f.pid:'unknown')}</small></span></button></td><td>${countryFlag(f.country,f.scope)}<button class="flow-destination" data-flow-id="${esc(f.id)}">${esc(f.remoteIp?f.remoteIp+(f.port?':'+f.port:''):f.domain)||'Unknown'}</button><small class="table-sub">${esc(f.domain)}${f.scope==='Local / reserved'?' · Local / reserved':''}</small></td><td><span class="tag blue">${esc(f.protocol)}</span><small class="table-sub">${esc(f.state)}</small></td><td>↓ ${sizeText(f.download)}<small class="table-sub">↑ ${sizeText(f.upload)}</small></td><td>${netRisk(indicator)}${lookup?`<button class="card-link" data-threat-indicator="${esc(indicator)}">Check ↗</button>`:''}</td><td class="mono">${time(f.last)}</td></tr>`;}).join('');}
else{$('#network-columns').innerHTML='<tr><th>APPLICATION</th><th>PROCESSES</th><th>FLOWS</th><th>DESTINATIONS</th><th>DOWNLOAD</th><th>UPLOAD</th></tr>';rows=s.apps.filter(a=>JSON.stringify(a).toLowerCase().includes(networkSearch));$('#network-rows').innerHTML=rows.slice(networkPageIndex*40,networkPageIndex*40+40).map(a=>`<tr><td><button class="app-cell" data-net-app="${esc(a.image)}">${appIcon(a.icon,a.name)}<span>${esc(a.name)}<small title="${esc(a.image)}">${esc(a.image)}</small></span></button></td><td>${a.pids.length}</td><td>${a.flows}</td><td>${a.destinations}</td><td>${sizeText(a.download)}</td><td>${sizeText(a.upload)}</td></tr>`).join('');}
if(!rows.length)$('#network-rows').innerHTML='<tr><td colspan="6" class="empty">No matching observations in this snapshot. Refresh after the app makes a connection.</td></tr>';$('#network-count').textContent=`${rows.length?networkPageIndex*40+1:0}–${Math.min(rows.length,networkPageIndex*40+40)} of ${rows.length} · 40 per page`;$('#network-warnings').innerHTML=s.warnings.map(w=>'<p>'+esc(w)+'</p>').join('');document.querySelector('[data-action=network-prev]').disabled=networkPageIndex===0;document.querySelector('[data-action=network-next]').disabled=(networkPageIndex+1)*40>=rows.length;}
function trafficChart(s){if(!s.apps.some(a=>a.download!=null||a.upload!=null))return `<section class="traffic-chart"><div class="card-head"><h2>Observed transfer</h2></div><p class="card-note">No measured transfer bytes in this snapshot. Administrator access adds ETW transfer tracing. Connections are still listed below.</p></section>`;const max=Math.max(1,...s.traffic.map(b=>Math.max(b.download,b.upload)));return `<section class="traffic-chart"><div class="card-head"><h2>Observed transfer</h2><span>↓ Download &nbsp; ↑ Upload</span></div><div class="traffic-bars">${s.traffic.map(b=>`<div title="${esc(date(b.at))} · Down ${sizeText(b.download)}, up ${sizeText(b.upload)}"><i style="height:${b.download/max*100}%"></i><b style="height:${b.upload/max*100}%"></b></div>`).join('')}</div><div class="chart-labels"><span>${time(s.start)}</span><span>Bytes per time bucket · ${time(s.capturedAt)}</span></div></section>`;}
function launchesPage(){return heading('Launch. Observe. Review.','Follow a game, installer or app from its exact process start.')+`<div class="launch-layout"><aside class="card launch-controls"><div class="card-body"><h2>Start a recording</h2><p class="card-note">For an in-game mod, select the game's executable. Use arguments if your launcher needs them.</p><label class="field">Application (.exe)<input id="launch-path" placeholder="C:\\Games\\Warcraft III\\Frozen Throne.exe"></label><button class="secondary" data-action="tracked-pick">Browse…</button><label class="field">Arguments · optional<input id="launch-arguments" placeholder="Launch arguments"></label><div class="warning-note">This runs the app on your PC with your desktop user's permissions. It is not a sandbox. Observe cannot guarantee malware detection.</div><button class="primary" id="launch-start" data-action="tracked-start">Launch & record</button><button class="secondary" data-action="tracked-finish" id="launch-finish">Finish recording</button><p class="small muted">Finish saves the evidence and leaves the app running. Compare a game before and after adding a mod; shared-process behavior cannot be attributed to a specific mod.</p></div><div class="card-head"><h2>Saved recordings</h2><button class="quiet" data-action="tracked-refresh">↻</button></div><div id="launch-list"></div></aside><section id="launch-detail" class="launch-detail"><div class="card empty">Choose a recording, or launch an app to begin.</div></section></div>`;}
function trackingControls(){if(page!=='launches')return;$('#launch-start').disabled=state.busy||state.data.gaming;$('#launch-finish').disabled=state.busy||!state.data.tracking;}
async function loadLaunches(){if(trackingLoading)return;trackingLoading=true;try{trackedList=await request('tracked-list');if(page==='launches'){renderLaunchList();if(trackedView)renderTracked();trackingControls();}}catch(e){toast(e.message,true);}finally{trackingLoading=false;}}
function renderLaunchList(){$('#launch-list').innerHTML=trackedList.map(r=>`<button class="launch-history ${trackedView?.run.id===r.id?'selected':''}" data-tracked-id="${esc(r.id)}"><strong>${esc(r.name)}</strong><small>${date(r.started)} · ${esc(r.status)}</small></button>`).join('')||'<p class="empty">No recordings yet.</p>';}
async function openTracked(id){trackedView=await request('tracked-open',{id});if(page==='launches'){renderTracked();renderLaunchList();}}
function renderTracked(){const {run:r,evidence:e,network:n}=trackedView;$('#launch-detail').innerHTML=`<section class="card"><div class="card-head"><div><h2>${esc(r.name)}</h2><small>${date(r.started)} · ${esc(r.status)} · root PID ${r.rootPid}</small></div><div class="inline-row"><button class="secondary" data-action="tracked-detail-refresh">↻ Refresh</button><button class="primary" data-action="tracked-chat">Ask chatGPT observer</button></div></div><div class="card-body"><p class="path-text">${esc(r.path)}</p><div class="launch-metrics"><div><strong>${e.processes.length}</strong>Processes</div><div><strong>${n.flows.length}</strong>Network flows</div><div><strong>${e.events.filter(x=>[2,11,12,13,14,23,26].includes(x.eventId)).length}</strong>File / registry records</div><div><strong>${e.findings.length}</strong>Review signals</div></div><p class="card-note">${e.findings.length?'Review the signals below. Legitimate software can produce the same behavior.':'No local rule matches in the captured evidence. This does not establish that the app is safe.'}</p><details><summary>File identity & coverage</summary><p class="mono path-text">SHA-256: ${esc(r.sha256)||'Not captured for this attached recording'}</p>${r.sha256?`<button class="secondary" data-threat-indicator="${esc(r.sha256)}">Check hash with ThreatFox</button>`:'<p class="card-note">Open the running process details to inspect its current executable hash and signature.</p>'}${r.warnings.map(w=>'<p class="card-note">'+esc(w)+'</p>').join('')}</details></div></section><section class="card"><div class="card-head"><h2>Behavior to review</h2></div><div class="card-body">${e.findings.map(f=>`<div class="finding"><span class="tag warn">${esc(f.severity)}</span><strong>${esc(f.title)}</strong><p>${esc(f.detail)}</p>${f.evidenceIds.map(id=>`<button class="card-link" data-tracked-event="${esc(id)}">Inspect event ↗</button>`).join('')}</div>`).join('')||'<p class="card-note">No rule matches. Review the process tree and destinations, and use ThreatFox for known indicators.</p>'}</div></section><section class="card"><div class="card-head"><h2>Process tree</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>PROCESS</th><th>PID / PARENT</th><th>STARTED</th><th>ENDED</th></tr></thead><tbody>${e.processes.map(p=>`<tr><td title="${esc(p.image)}">${p.parentPid?'↳ ':''}${esc(p.image.split('\\').at(-1))}</td><td>${p.pid}${p.parentPid?' / '+p.parentPid:' · root'}</td><td>${time(p.started)}</td><td>${p.ended?time(p.ended):'Not recorded'}</td></tr>`).join('')}</tbody></table></div></section><section class="card"><div class="card-head"><h2>Connections & DNS</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>APP</th><th>DESTINATION</th><th>PROTOCOL</th><th>INTELLIGENCE</th></tr></thead><tbody>${n.flows.slice(0,150).map(f=>{const indicator=f.remoteIp||f.domain.split(' (')[0];return `<tr><td>${esc(f.process)} · ${esc(f.pid)}</td><td>${esc(f.remoteIp+(f.port?':'+f.port:''))}<small class="table-sub">${esc(f.domain)}</small></td><td>${esc(f.protocol)}</td><td>${netRisk(indicator)}${indicator&&f.scope!=='Local / reserved'?`<button class="card-link" data-threat-indicator="${esc(indicator)}">Check ↗</button>`:''}</td></tr>`;}).join('')||'<tr><td colspan="4" class="empty">No linked connections recorded.</td></tr>'}</tbody></table></div></section><section class="card"><div class="card-head"><h2>Recorded changes & events</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>TIME</th><th>PROCESS</th><th>EVENT</th><th>DETAIL</th></tr></thead><tbody>${e.events.filter(x=>!x.isNetwork).slice(-250).reverse().map(x=>`<tr><td>${time(x.timestamp)}</td><td>${esc(x.process)}</td><td><button data-tracked-event="${esc(x.id)}">${esc(x.kind)}</button></td><td title="${esc(x.detail)}">${esc(x.detail)}</td></tr>`).join('')}</tbody></table></div><div class="table-foot">Latest 250 non-network events · Recording remains on this PC</div></section>`;}
function threatFoxCard(){return `<article class="plugin-card"><div class="plugin-top"><div class="plugin-logo fox-logo">♢</div><div><h2>ThreatFox</h2><small>KNOWN MALWARE INDICATORS</small></div></div><p>Look up a destination or executable hash in abuse.ch ThreatFox. Review matches alongside your recorded evidence.</p><ul><li>Disabled until you configure an Auth-Key</li><li>Lookups only when you click Check</li><li>Encrypted key · no file uploads or submissions</li></ul><div class="warning-note">A lookup shares the selected public IP, domain or hash with ThreatFox. No match does not establish safety.</div><div class="plugin-bottom"><span class="tag" id="threatfox-state">Not configured</span><div class="inline-row"><button class="secondary" data-action="threatfox-remove">Remove</button><button class="primary" data-action="threatfox-config">Configure ↗</button></div></div><button class="card-link" data-action="threatfox-manual">Look up an indicator →</button></article>`;}
function threatFoxConfig(){modal('Configure ThreatFox',`<p class="card-note">Get an Auth-Key from auth.abuse.ch. Observe uses the ThreatFox Community API. Nothing is looked up automatically.</p><label class="field">ThreatFox Auth-Key<input id="threatfox-key" type="password" autocomplete="off" placeholder="${state.data.plugins.hasThreatFoxKey?'Leave blank to keep saved key':'Enter Auth-Key'}"></label><div class="warning-note">Each Check shares only the selected indicator with ThreatFox. It does not upload executable files or submit new indicators.</div>`,`<button class="primary" data-action="threatfox-save">Save & enable</button>`);}
async function checkThreat(indicator){if(!state.data.plugins.threatFox){threatFoxConfig();return;}const r=await request('threatfox-search',{indicator});threatChecks.set(r.indicator,r);if(page==='network')renderNetwork();if(page==='launches'&&trackedView)renderTracked();modal('ThreatFox · '+(r.status==='match'?'Indicator match':'No match'),`<p class="mono path-text">${esc(r.indicator)}</p><p class="card-note">Checked ${date(r.checkedAt)}</p><div class="warning-note">${esc(r.note)}</div>${r.matches.map(m=>`<section class="finding"><h3>${esc(m.malware)||'Unlabelled malware'}</h3><p>${esc(m.threat)}</p><p class="mono path-text">${esc(m.ioc)}</p><p>Confidence: ${m.confidence}% · First seen ${esc(m.firstSeen)} · Last seen ${esc(m.lastSeen)||'unavailable'}</p></section>`).join('')}`);}
document.addEventListener('input',e=>{if(e.target.id==='net-search'){networkSearch=e.target.value.toLowerCase();networkPageIndex=0;renderNetwork();}});
document.addEventListener('change',e=>{if(e.target.id==='net-period'){networkMinutes=Number(e.target.value);loadNetwork();}});
document.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;try{
if(b.dataset.netTab){networkTab=b.dataset.netTab;networkPageIndex=0;renderNetwork();return;}if(b.dataset.netApp){networkApp=b.dataset.netApp;networkTab='flows';networkPageIndex=0;renderNetwork();return;}if(b.dataset.flowId){jsonModal('Flow evidence & attribution',networkSnapshot.flows.find(f=>f.id===b.dataset.flowId));return;}if(b.dataset.trackedId){await openTracked(b.dataset.trackedId);return;}if(b.dataset.trackedEvent){jsonModal('Original recorded evidence',trackedView.evidence.events.find(x=>x.id===b.dataset.trackedEvent));return;}if(b.dataset.threatIndicator){await checkThreat(b.dataset.threatIndicator);return;}
switch(b.dataset.action){case 'network-refresh':await loadNetwork();break;case 'network-clear-app':networkApp='';networkPageIndex=0;renderNetwork();break;case 'network-prev':networkPageIndex=Math.max(0,networkPageIndex-1);renderNetwork();break;case 'network-next':networkPageIndex++;renderNetwork();break;case 'country-import':await request('country-import');await loadNetwork();break;
case 'tracked-pick':const picked=await request('tracked-pick');if(picked.path)$('#launch-path').value=picked.path;break;case 'tracked-start':b.disabled=true;try{const run=await request('tracked-start',{path:$('#launch-path').value,arguments:$('#launch-arguments').value});await loadLaunches();await openTracked(run.id);}finally{trackingControls();}break;case 'tracked-finish':toast((await request('tracked-finish',trackedView?.run.status==='recording'?{id:trackedView.run.id}:{})).message);await loadLaunches();if(trackedView)await openTracked(trackedView.run.id);break;case 'tracked-refresh':await loadLaunches();break;case 'tracked-detail-refresh':await openTracked(trackedView.run.id);break;case 'tracked-chat':observerChat=await request('tracked-chat',{id:trackedView.run.id});observerMinutes=60;observerDraft='Explain this recorded run. Review child processes, IP calls, and file or registry changes that could be suspicious. Separate observations from uncertainty.';go('observer');break;
case 'threatfox-config':threatFoxConfig();break;case 'threatfox-save':await request('threatfox-save',{key:$('#threatfox-key').value});$('#threatfox-key').value='';$('#modal').classList.add('hidden');toast('ThreatFox enabled. Lookups remain manual.');break;case 'threatfox-remove':await request('threatfox-remove');threatChecks.clear();toast('ThreatFox disabled and key removed.');break;case 'threatfox-manual':modal('Check an indicator','<label class="field">Public IP, domain or SHA-256<input id="threatfox-indicator"></label><p class="card-note">Clicking Check shares this indicator with ThreatFox.</p>','<button class="primary" data-action="threatfox-check">Check</button>');break;case 'threatfox-check':await checkThreat($('#threatfox-indicator').value);break;}
}catch(error){toast(error.message,true);}});
